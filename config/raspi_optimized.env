# ========================================
# RASPBERRY PI 4 OPTIMIZED CONFIGURATION
# Content Empire Mode (3 agents)
# ========================================

# --- API Keys ---
GROQ_API_KEY=your_groq_api_key_here

# --- Evolution Settings (Minimal) ---
POPULATION_SIZE=3
GENERATIONS=100
ELITE_SIZE=1
MUTATION_RATE=0.15
CROSSOVER_RATE=0.7

# --- Agent Settings (Lightweight) ---
GROQ_MODEL=llama-3.1-8b-instant  # Fastest, cheapest
MAX_TOKENS=500                    # Shorter responses
TEMPERATURE=0.8

# --- Evolution Engine (Use Groq instead of DeepSeek) ---
USE_GROQ_FOR_EVOLUTION=true
EVOLUTION_MODEL=llama-3.1-8b-instant
EVOLUTION_TEMPERATURE=0.9

# --- Flask Settings ---
FLASK_PORT=5000
FLASK_DEBUG=false
FLASK_HOST=0.0.0.0

# --- Logging (Reduce I/O on SD card) ---
LOG_LEVEL=WARNING
LOG_FILE=/tmp/hive.log  # Use tmpfs to avoid SD wear

# --- ChromaDB Settings ---
CHROMA_PERSIST_DIR=/tmp/chroma_db  # Use tmpfs for speed
CHROMA_COLLECTION=hive_memory

# --- Performance Tuning ---
WORKER_THREADS=2              # Low for Raspi CPU
CACHE_ENABLED=true
CACHE_SIZE=50                 # Small cache

# --- D8-GENESIS (Disabled for Raspi) ---
ENABLE_D8_GENESIS=false       # Requires DeepSeek (too heavy)
CODE_VAULT_ENABLED=false

# --- Cost Optimization ---
# Expected monthly cost: $8-15 with this config
# - Groq actions: ~$5-10
# - Groq evolution: ~$3-5
# - Raspi power: ~$2

# --- Notes ---
# 1. Use Raspi 4 8GB for best performance
# 2. Enable swap if using 4GB model:
#    sudo dphys-swapfile swapoff
#    sudo nano /etc/dphys-swapfile  # Set CONF_SWAPSIZE=2048
#    sudo dphys-swapfile setup
#    sudo dphys-swapfile swapon
#
# 3. Mount tmpfs for logs (avoid SD card wear):
#    sudo mount -t tmpfs -o size=100M tmpfs /tmp
#
# 4. Monitor resources:
#    htop
#    free -h
#    df -h
